A little repo to illustrate css duplication with webpack extract text plugin

npm i
npm run build

check index.css

It works perfectly without less-loader (vanilla-css branch) but duplicates css output with less-loader (master)
